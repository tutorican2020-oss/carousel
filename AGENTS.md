# AGENTS.md

## Cursor Cloud specific instructions

This repository (`carousel`) is currently an empty/skeleton project with only a `README.md`. There is no application code, no dependency manifests, no build system, and no services to run.

- **No dependency installation** is required at this time.
- **No services** need to be started.
- **No lint, test, or build commands** are configured yet.

When application code is added, update this file with relevant startup caveats, service descriptions, and non-obvious development notes.
