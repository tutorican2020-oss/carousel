# AGENTS.md

## Cursor Cloud specific instructions

This repository ("carousel") is currently an empty scaffold with only a `README.md`. There are no application services, dependencies, build steps, linters, or tests to run.

When application code is added in the future, update this section with:
- How to install dependencies and which package manager to use.
- How to run the dev server, tests, and linter.
- Any non-obvious startup caveats or environment requirements.
